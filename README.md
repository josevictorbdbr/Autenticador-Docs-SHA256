# Autenticador de arquivos SHA-256

Projeto desenvolvido em python usando uma implementação própria do algoritimo SHA-256

### Como rodar:

- Execute o codigo do autenticador

- Gerar o hash SHA-256 de um arquivo(pdf, txt, etc...)

- Verificar a autenticidade do arquivo usando o hash gerado
